{
  "name": "catalogo-modelos-ia",
  "type": "module",
  "version": "1.0.0",
  "description": "Catálogo de modelos de IA para programación, agrupados por empresa.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "tsx": "^4.20.4"
  }
}
